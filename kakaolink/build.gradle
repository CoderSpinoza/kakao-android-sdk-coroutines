apply from: "$rootDir/libraries.gradle"

dependencies {
    api project(':network')
    api project(':message-template')
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        jvmTarget = '1.8'
    }
}